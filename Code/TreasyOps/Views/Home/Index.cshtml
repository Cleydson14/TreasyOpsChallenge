@model  TreasyOps.ViewModel.ResultViewModel
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <div class="col-sm-12">
        <div class="col-sm-9">
            <h1>Treasy Ops Challenge</h1>
        </div>
        <div class="col-sm-3">
            <div class="pull-right">
                @Html.ActionLink("Atualizar", "Index", "Home", new { @class = "btn btn-primary" })
            </div>
        </div>
    </div>

    @using (Html.BeginForm("Index", "Home", FormMethod.Get))
    {
        <div class="clearfix"></div>
        <div class="form-group row">
            <div class="col-xs-4">
                <label for="startWeek">Semana Inicial:</label>
                @Html.TextBox("startWeek", (int)ViewBag.StartWeek, new { @class = "form-control", type = "number", min = "1", max = "52" })
            </div>

            <div class="col-xs-4">
                <label for="endWeek">Semana Final:</label>
                @Html.TextBox("endWeek", (int)ViewBag.EndWeek, new { @class = "form-control", type = "number", min = "1", max = "52" })
            </div>
        </div>

        <div class="container-fluid">
            <button type="submit" class="btn btn-success">Filtrar</button>
        </div>
    }
</div>

<ul class="nav nav-tabs">
    <li class="active"><a data-toggle="tab" href="#question1">Questão 1</a></li>
    <li><a data-toggle="tab" href="#question2">Questão 2</a></li>
</ul>

<div class="tab-content">
    <div id="question1" class="tab-pane fade in active">
        <h3>Questão 1</h3>
        <p>Abaixo, listamos a quantidade de negócios por etapa dividindo semanalmente no ano de 2017.</p>
        <table class="table table-bordered table-striped">
            <tbody>
                @foreach (var week in Model.Question1)
                {
                    <tr>
                        <td>Semana: <strong>@week.WeekOfYear.ToString("D2")</strong></td>
                    </tr>
                    <tr>
                        <td>
                            <table class="table table-bordered table-hover table-striped">
                                <thead>
                                    <tr>
                                        <th width="90%">Etapa</th>
                                        <th width="10%" class="text-right">Quantidade</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in week.Items)
                                    {
                                        <tr>
                                            <td>@item.StageTo</td>
                                            <td class="text-right">@item.Count.ToString("D2")</td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <td class="text-right">TOTAL: </td>
                                        <td class="text-right"><strong>@week.TotalItems.ToString("D2")</strong></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div id="question2" class="tab-pane fade">
        <h3>Questão 2</h3>
        <p>Abaixo, listamos o total de negócios por semana no ano de 2017.</p>
        <table class="table table-bordered table-striped">
            <tbody>
                @foreach (var week in Model.Question2)
                {
                    <tr>
                        <td>Semana: <strong>@week.WeekOfYear.ToString("D2")</strong></td>
                    </tr>
                    <tr>
                        <td>
                            <table class="table table-bordered table-hover table-striped">
                                <thead>
                                    <tr>
                                        <th width="90%">Etapa</th>
                                        <th width="10%" class="text-right">Quantidade</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in week.Items)
                                    {
                                        <tr>
                                            <td>@item.StageTo</td>
                                            <td class="text-right">@item.Count.ToString("D2")</td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <td class="text-right">TOTAL: </td>
                                        <td class="text-right"><strong>@week.TotalItems.ToString("D2")</strong></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>